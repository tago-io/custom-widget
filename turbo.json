{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["build"]
    },
    "check:types": {
      "dependsOn": ["^build"]
    },
    "lint": {},
    "format": {}
  }
}
